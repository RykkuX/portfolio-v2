# Hooks Directory

This directory contains custom React hooks that can be reused across components.

## Usage

```tsx
import { useCustomHook } from '../hooks/useCustomHook';
```

## Examples

- `useLocalStorage` - For managing local storage
- `useMediaQuery` - For responsive design
- `useScrollPosition` - For scroll-based animations
- `useTypingEffect` - For typing animations 